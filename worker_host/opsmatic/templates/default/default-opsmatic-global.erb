# auomatically generated by chef cookbook opsmatic-cookbook
export OPSMATIC_INTEGRATION_TOKEN="<%= node["opsmatic"]["integration_token"] %>"
<% if node["opsmatic"]["api_http"] && !node["opsmatic"]["api_http"].empty? %>
export OPSMATIC_API_HTTP="<%= node["opsmatic"]["api_http"] %>"
<% end %>

<% if node["opsmatic"]["env_files"] %>
# custom local includes
<% node["opsmatic"]["env_files"].each do |file|  %>
source <%= file %>
<% end %>
<% end %>
