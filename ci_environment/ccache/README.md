ccache Cookbook
===============
Installs ccache

Usage
-----
#### ccache::default

Just include `ccache` in your node's `run_list`:

```json
{
  "name":"my_node",
  "run_list": [
    "recipe[ccache]"
  ]
}
```
