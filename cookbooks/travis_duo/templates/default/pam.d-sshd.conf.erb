# Managed by Chef on <%= node.name %> :heart:
# cookbook:: travis_duo
#     file:: templates/default/pam.d-sshd.conf.erb

auth  [success=1 default=ignore] pam_duo.so
auth  requisite pam_deny.so
auth  required pam_permit.so
auth  optional pam_cap.so
