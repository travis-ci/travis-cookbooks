<% unless @jdk.nil? || @jdk.empty? %>
if [[ -d <%= @path =%>/<%= @jdk =%> ]]; then
  export JAVA_HOME=<%= @path =%>/<%= @jdk %>
  export PATH="$JAVA_HOME/bin:$PATH"
fi
export _JAVA_OPTIONS='-Xmx2048m -Xms512m'
export MALLOC_ARENA_MAX=2
<% end %>
