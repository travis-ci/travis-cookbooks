# Managed by Chef for <%= node.name %> :heart_eyes_cat:

[client]
default-character-set = utf8
port = 3306
user = root
password = <%= @password %>
socket = /run/mysql-5.6/mysqld.sock

[mysql]
default-character-set = utf8
